# Justa colors :sparkles:
Pacote de cores utilizados pela justa. 
Para instalar o pacote em seu repositório basta digitar em seu console:
```bash
 npm install jst-colors ou yarn install jst-colors
```

